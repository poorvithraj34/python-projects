#set.union()-this like add all theb elements in the set leaving the reapting one
#set.intersection()-this give the common elements of both sets

set1={1,2,3}
set2={3,4,5}
print(set1.intersection(set2))
